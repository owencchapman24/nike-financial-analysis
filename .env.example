# SEC requests must identify the automated tool. Replace this placeholder only in
# the ignored local .env file; never put a real contact address in this example.
SEC_USER_AGENT="Nike financial analysis portfolio project your-contact-address@example.com"
